
function sortNumbers(arrayOfNumbers) {
    // #1 sort the numbers in ascending order with [].sort()
    // arrayOfNumbers.sort((a, b) => a - b)

    // #2 sort the numbers in ascending order with for
    // TODO

    // #3 sort the numbers in ascending order with [].reduce(callback)
    // TODO

    return arrayOfNumbers
}

// challenge #2
function getUniqueValues(arrayOfValues) {
    // do with utils that you prefer the most
    return arrayOfValues
}

// challenge #3
function countProvidedValues(arrayOfValues) {
    // do with utils that you prefer the mos
    return arrayOfValues
}

module.exports = {
    sortNumbers,
    getUniqueValues,
    countProvidedValues,
}